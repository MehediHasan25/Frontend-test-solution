import { hostIp } from './IpAddress';

// Login API
export const loginAPI = hostIp + `login`;

// Devices API
export const devicesAPI = hostIp + `devices`;

// Notify API
export const notifyAPI = hostIp + `notify`;